# deps / build
node_modules
dist
build
coverage

# logs
*.log

# env & secrets (KHÔNG commit)
.env
.env.local
.env.*.local
secrets
*.pem
*.key

# Credential/data JSON lạ (vd service account) — giữ lại config json của project
*.json
!package.json
!package-lock.json
!tsconfig.json
!vercel.json
# Schema tham chiếu cho Claude market research output (KHÔNG phải secret)
!data/market-research/output.schema.json
# Danh sách ad_id AI vision loại (config lọc, KHÔNG phải secret) — pipeline cần
!scripts/vision_excludes.json
# Output Claude tạo ra (có thể chứa dữ liệu) — KHÔNG commit
data/market-research/market_research_output.json

# Vercel
.vercel

# OS
.DS_Store

# Cho phép template env (không phải secret)
!.env.example

# Script tạm (readback/debug) — không commit
_*.mjs
_*.json

.vercel
.env*

# Artifact báo cáo tuần (runtime, không commit)
report-input.json
report-out.json
scripts/logs/
